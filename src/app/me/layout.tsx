export const metadata = {
  title: "설정",
};

export default function MeLayout({children}: { children: React.ReactNode }) {
  return <div style={{margin: "0px", padding: "0 4px", background: "#fff"}}>{children}</div>;
}